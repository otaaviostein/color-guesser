import ScoreDetails from './ScoreDetails';
import ProgressBar from './ProgressBar';

const ScoreBoard = () => {
  return (
    <>
      <ScoreDetails />
      <ProgressBar />
    </>
  );
};

export default ScoreBoard;
